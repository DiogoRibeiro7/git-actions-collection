[package]
name = "rust-crate"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Minimal Rust crate used to exercise reusable GitHub Actions workflows."
repository = "https://github.com/DiogoRibeiro7/git-actions-collection"
readme = "README.md"

[features]
default = []
extra = []

[dependencies]

[dev-dependencies]
criterion = "0.7.0"

[[bench]]
name = "add"
harness = false
