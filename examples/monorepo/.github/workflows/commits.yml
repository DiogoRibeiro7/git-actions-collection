name: Commits

on:
  pull_request:

jobs:
  lint:
    permissions:
      contents: read
      pull-requests: read
    uses: DiogoRibeiro7/git-actions-collection/.github/workflows/conventional-commits.yml@v1
