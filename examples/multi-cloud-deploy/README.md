# Multi-Cloud Deployment Example

This example shows how to call the reusable **Multi-Cloud Deploy** workflow.
It includes Terraform, Pulumi, and Bicep samples for AWS, GCP, and Azure respectively.

## Workflow
See `.github/workflows/deploy.yml` for how to invoke the workflow with Terraform.
Adjust inputs to use `pulumi` or `bicep` as needed.
