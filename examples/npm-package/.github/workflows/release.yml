name: Release

on:
  push:
    tags:
      - 'v*'

jobs:
  publish:
    uses: DiogoRibeiro7/git-actions-collection/.github/workflows/npm-publish.yml@develop
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
