name: Terraform Plan

on:
  pull_request:
  push:
    branches: [ main ]

permissions:
  contents: read
  pull-requests: write

jobs:
  plan:
    uses: DiogoRibeiro7/git-actions-collection/.github/workflows/terraform-plan-comment.yml@develop
    with:
      working-directory: .
