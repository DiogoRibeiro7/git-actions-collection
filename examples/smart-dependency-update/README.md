# Smart Dependency Update Example

This example demonstrates the `smart-dependency-update` action across Node and Python projects.

## Workflow

See `.github/workflows/dependency-update.yml` for usage.
