name: dependency-review

on:
  pull_request:

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    uses: your-org/gh-actions-collection/.github/workflows/dependency-review.yml@main
