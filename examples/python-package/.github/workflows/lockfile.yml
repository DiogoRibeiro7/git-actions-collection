name: lockfile

on:
  pull_request:
    paths:
      - "pyproject.toml"
      - "poetry.lock"

jobs:
  check:
    permissions:
      contents: read
    uses: DiogoRibeiro7/git-actions-collection/.github/workflows/lockfile-consistency.yml@v1
