name: lockfile

on:
  pull_request:
    paths:
      - "pyproject.toml"
      - "poetry.lock"

jobs:
  check:
    uses: DiogoRibeiro7/git-actions-collection/.github/workflows/lockfile-consistency.yml@develop
