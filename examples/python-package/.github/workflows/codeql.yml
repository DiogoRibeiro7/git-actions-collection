name: codeql

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read
  security-events: write

jobs:
  analyze:
    uses: DiogoRibeiro7/git-actions-collection/.github/workflows/codeql-analysis.yml@develop
