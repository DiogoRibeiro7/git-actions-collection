# Calls npm-publish.yml (compatibility alias) to install dependencies, run any build script, and
# publish the repo-root package to npm with provenance under the latest dist-tag.
name: Publish to npm (compatibility alias)

on:
  workflow_call:
    inputs:
      node-version:
        description: 'Node.js version'
        required: false
        type: string
        default: '24'
    secrets:
      NPM_TOKEN:
        required: true

permissions:
  contents: read
  id-token: write

jobs:
  publish:
    uses: ./.github/workflows/npm-publish.yml
    with:
      node-version: ${{ inputs.node-version }}
      working-directory: .
      tag: latest
    secrets:
      NPM_TOKEN: ${{ secrets.NPM_TOKEN }}
