# Builds a Python package with python -m build or Poetry, validates it with twine check --strict,
# and publishes via PyPI trusted publishing, skipping existing versions.
name: Publish to PyPI
on:
  workflow_call:
    inputs:
      python-version:
        description: 'Python version to use for build/publish'
        required: false
        type: string
        default: '3.12'
      build-backend:
        description: 'build | poetry'
        required: false
        type: string
        default: 'build'
      working-directory:
        description: 'Directory containing the Python package'
        required: false
        type: string
        default: '.'
      environment:
        description: 'Deployment environment name (for protection rules)'
        required: false
        type: string
        default: 'pypi'
      pre-release:
        description: 'Publish as a pre-release (true/false)'
        required: false
        type: boolean
        default: false
      pip-version:
        description: "pip release to install (set to 'latest' to track upstream)"
        required: false
        type: string
        default: '26.2.1'
permissions:
  contents: read
  id-token: write  # OIDC for PyPI trusted publishing
jobs:
  publish:
    environment: ${{ inputs.environment }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ${{ inputs.working-directory }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - uses: actions/setup-python@ece7cb06caefa5fff74198d8649806c4678c61a1
        with:
          python-version: ${{ inputs.python-version }}
          cache: pip
      - name: Install build backend
        run: |
          set -euo pipefail
          pip_version="${{ inputs.pip-version }}"
          if [ "$pip_version" = "latest" ]; then
            python -m pip install --upgrade pip
          else
            python -m pip install --upgrade "pip==$pip_version"
          fi
          if [ "${{ inputs.build-backend }}" = "poetry" ]; then
            pip install poetry twine
          else
            pip install build twine
          fi
      - name: Build
        run: |
          if [ "${{ inputs.build-backend }}" = "poetry" ]; then
            poetry build
          else
            python -m build
          fi
      - name: Validate distribution metadata
        run: twine check --strict dist/*

      - name: Publish to PyPI (trusted publishing)
        uses: pypa/gh-action-pypi-publish@ed0c53931b1dc9bd32cbe73a98c7f6766f8a527e
        with:
          skip-existing: true
          verbose: true
          print-hash: true
          repository-url: ${{ inputs.pre-release && 'https://test.pypi.org/legacy/' || '' }}
