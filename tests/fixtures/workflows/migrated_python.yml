name: Python package
"on":
  push:
    branches: [main]
  pull_request:

jobs:
  ci:
    uses: DiogoRibeiro7/gh-actions-collection/.github/workflows/python-test-matrix.yml@main
    with:
      python-versions: '["3.x"]'
